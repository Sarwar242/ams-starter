[supervisord]
nodaemon=true
user=root
logfile=/var/log/supervisor/supervisord.log
pidfile=/var/run/supervisord.pid

[unix_http_server]
file=/var/run/supervisor.sock
chmod=0700

[supervisorctl]
serverurl=unix:///var/run/supervisor.sock

[rpcinterface:supervisor]
supervisor.rpcinterface_factory = supervisor.rpcinterface:make_main_rpcinterface

# Supervisor logs
[program:apache2]
command=/bin/bash -c "source /etc/apache2/envvars && exec /usr/sbin/apache2 -D FOREGROUND"
numprocs=1
autostart=true
autorestart=true
startsecs=10
stopwaitsecs=10
stdout_logfile=/var/log/supervisor/apache2.log
stderr_logfile=/var/log/supervisor/apache2_error.log

# Optional: Queue worker (uncomment if using Laravel queues)
# [program:laravel-worker]
# process_name=%(program_name)s_%(process_num)02d
# command=php /var/www/html/artisan queue:work --sleep=3 --tries=3
# autostart=true
# autorestart=true
# numprocs=1
# stdout_logfile=/var/log/supervisor/laravel-worker.log
# stderr_logfile=/var/log/supervisor/laravel-worker_error.log
# stopwaitsecs=60

# Optional: Cron scheduler
# [program:laravel-scheduler]
# command=php /var/www/html/artisan schedule:work
# autostart=true
# autorestart=true
# stdout_logfile=/var/log/supervisor/laravel-scheduler.log
# stderr_logfile=/var/log/supervisor/laravel-scheduler_error.log
# startsecs=0
# stopwaitsecs=60
